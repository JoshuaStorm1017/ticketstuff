{
  "name": "ticketstuff",
  "version": "1.0.0",
  "description": "A free, self-hosted Zendesk and Help Scout alternative.",
  "license": "MIT",
  "type": "module",
  "private": false,
  "scripts": {
    "dev": "node server/index.js",
    "start": "node server/index.js",
    "lint": "node scripts/lint.js",
    "typecheck": "node --check server/index.js && node --check server/routes.js && node --check server/store.js && node --check server/domain.js && node --check public/app.js && node --check scripts/build.js && node --check scripts/lint.js && node --check scripts/secret-scan.js && node --check tests/domain.test.js && node --check tests/api.test.js",
    "test": "node --test",
    "build": "npm run typecheck && npm run lint && node scripts/build.js",
    "secret-scan": "node scripts/secret-scan.js"
  },
  "engines": {
    "node": ">=24"
  }
}
